// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '8.2.1' apply false // Use a recent version
    id 'com.android.library' version '8.2.1' apply false // Use a recent version
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
